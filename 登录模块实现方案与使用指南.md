# 仓库管理系统登录模块实现方案与使用指南

## 一、系统架构设计

### 1. 实体层设计
- **User**: 用户实体，包含用户基本信息与账号认证信息
- **Role**: 角色实体，定义了系统中不同的用户角色（超级管理员、城市运营商、代理商等）
- **Permission**: 权限实体，定义了系统中的各种权限
- **RolePermission**: 角色-权限关联实体，实现多对多关系

### 2. 安全架构设计
- **JWT认证**: 使用无状态JWT令牌进行用户认证
- **权限控制**: 基于Spring Security实现方法级和API级的权限控制
- **自定义表达式**: 通过CustomSecurityExpression类实现细粒度权限判断

### 3. 数据访问层
- **UserRepository**: 用户数据访问接口
- **RoleRepository**: 角色数据访问接口

## 二、工作流程

### 1. 用户登录流程
1. 用户提交用户名和密码到 `/api/auth/login` 端点
2. 后端验证用户名和密码
3. 验证成功后，生成JWT令牌
4. 返回令牌和用户信息给前端
5. 前端保存令牌用于后续请求

### 2. 权限验证流程
1. 用户在请求中携带JWT令牌
2. `JwtAuthenticationFilter` 拦截请求并验证令牌
3. 验证成功后，从令牌中提取用户信息
4. 通过`CustomSecurityExpression`检查当前用户是否有权限执行请求的操作
5. 权限验证通过后，请求被转发到对应的控制器方法

## 三、接口说明

### 1. 认证接口
- **登录**: `POST /api/auth/login`
  - 请求体: `{ "username": "用户名", "password": "密码" }`
  - 响应: `{ "token": "JWT令牌", "type": "Bearer", "id": 用户ID, "username": "用户名", "roles": ["角色1", "角色2"] }`

- **注册**: `POST /api/auth/register`
  - 请求体: `{ "username": "用户名", "password": "密码", "email": "电子邮件", "phone": "电话号码" }`
  - 响应: `"User registered successfully!"`或错误信息

### 2. 权限使用方式
- **服务层方法权限控制**: 使用`@PreAuthorize("@customSecurityExpression.hasPermission('XXX_YYY')")`注解
- **API请求权限控制**: 在SecurityConfig中配置

## 四、各类角色权限设计

角色体系设计如下（可根据业务需求调整）:

1. **超级管理员(SUPER_ADMIN)**
   - 拥有所有权限
   - 可以管理所有仓库和用户

2. **城市运营商(CITY_OPERATOR)**
   - 管理区域仓库
   - 可查看、编辑区域内的库存、订单、车辆等

3. **代理商(AGENT)**
   - 负责商品调拨
   - 可以管理分配给自己的订单和库存

4. **供应商(SUPPLIER)**
   - 商品供应
   - 可以查看和管理自己提供的商品

5. **门店(STORE)**
   - 终端销售
   - 可以创建订单、查看库存等

## 五、使用指南

### 1. 前端使用示例

```javascript
// 登录请求示例
async function login(username, password) {
  const response = await fetch('/api/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username, password })
  });
  
  const data = await response.json();
  
  if (response.ok) {
    // 保存令牌到本地存储
    localStorage.setItem('token', data.token);
    localStorage.setItem('user', JSON.stringify({
      id: data.id,
      username: data.username,
      roles: data.roles
    }));
    return data;
  } else {
    throw new Error(data.message || '登录失败');
  }
}

// 发送带认证的请求示例
async function fetchWithAuth(url, options = {}) {
  const token = localStorage.getItem('token');
  
  const headers = {
    ...options.headers,
    'Authorization': `Bearer ${token}`
  };
  
  const response = await fetch(url, {
    ...options,
    headers
  });
  
  if (response.status === 401) {
    // 令牌过期或无效，重定向到登录页
    localStorage.removeItem('token');
    window.location.href = '/login';
    return;
  }
  
  return response;
}
```

### 2. 后端权限管理示例

```java
// 控制器方法权限控制示例
@RestController
@RequestMapping("/api/products")
public class ProductController {

    @PostMapping
    @PreAuthorize("@customSecurityExpression.hasPermission('PRODUCT_CREATE')")
    public ResponseEntity<Product> createProduct(@RequestBody Product product) {
        // 创建产品的逻辑
    }
    
    @GetMapping("/{id}")
    @PreAuthorize("@customSecurityExpression.hasPermission('PRODUCT_VIEW')")
    public ResponseEntity<Product> getProduct(@PathVariable Long id) {
        // 获取产品的逻辑
    }
}
```

## 六、系统安全注意事项

1. **密码安全**:
   - 用户密码使用BCrypt进行加密存储
   - 不在任何日志中记录密码

2. **令牌安全**:
   - JWT密钥保持安全，不应泄露
   - 令牌设置合理的过期时间

3. **权限检查**:
   - 确保所有敏感操作都有权限检查
   - 前端和后端都实施权限控制

4. **日志记录**:
   - 记录所有认证和授权操作
   - 监控异常登录尝试

## 七、扩展建议

1. **添加刷新令牌机制**:
   实现令牌刷新功能，延长用户会话有效期

2. **实现角色管理界面**:
   为超级管理员提供角色和权限管理功能

3. **添加数据权限控制**:
   基于用户角色实现数据行级访问控制

4. **实现用户信息管理**:
   允许用户修改个人信息、重置密码等

5. **双因素认证**:
   为敏感操作添加双因素认证机制 